export const selectFavoritesId = (state) => state.favorites.favoritesId;

export const selectTeachersIsFavorite = (state) => state.favorites.teachersIsFavorite;

export const selectLastKey = (state) => state.favorites.lastKey;

export const selectLoadMore = (state) => state.favorites.loadMore;

export const selectLoading = (state) => state.favorites.loading;

export const selectError = (state) => state.teachers.error;
